name: robot-tests
on:
  workflow_run:
    workflows: ["Docker Image CI"]
    branches: [wasm-main]
    types: 
      - completed

jobs:

  build:

    runs-on: rnd201

    steps:
    - name: info
      run: git status
      run: git switch tests
      run: git fetch
    - name: start robot tests
      run: xvfb-run -n 56 -e /dev/stdout -s '-screen 0 1366x768x24' -f /home/runner/.Xauthority sh -c 'startlxde& /home/runner/run_test.sh'
