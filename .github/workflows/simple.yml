name: mytries
on:
  workflow_run:
    workflows: ["Docker Image CI"]
    branches: [wasm-main]
    types: 
      - completed

jobs:

  build:

    runs-on: rnd201

    steps:
    - name: info
      run: git status
